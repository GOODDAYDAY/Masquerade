/**
 * Remotion entry point — registers the root component.
 * This file is the entry for Remotion's bundler (separate from Vite).
 */

import { registerRoot } from "remotion";
import { Root } from "./Root";

registerRoot(Root);
